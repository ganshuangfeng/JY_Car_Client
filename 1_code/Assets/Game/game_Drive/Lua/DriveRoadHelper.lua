-- 游戏车辆类
local basefunc = require "Game/Common/basefunc"
DriveRoadHelper = basefunc.class()
local M = DriveRoadHelper
M.name = "DriveRoadHelper"

function M.splicing_range(pos,range,shun_or_ni)

end 

function M.get_image_by_pos(pos,range,shun_or_ni)
    
end

function M.set_image_scale(pos,range,shun_or_ni)
    
end